import { ENV } from "../app/models/model";

export const environment: any = {
    production: false,
    apiURL: "http://localhost:5000/api/",
    userEmail: null,
    role: null,
    userID: null,
    username: null,
};
